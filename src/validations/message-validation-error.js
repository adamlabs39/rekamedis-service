const REQUIRED = "tidak boleh kosong";
const rekamMedisUuidRequired = `${REQUIRED}`;
const idRequired = `${REQUIRED}`;
const alasanRequired = `${REQUIRED}`;
const sessionIdRequired = `${REQUIRED}`;
const keyRequired = ` seperti 'alergi' ${REQUIRED}`;
const messageRequired = `${REQUIRED}`;
const nameRequired = `${REQUIRED}`;
const userUuidRequired = `${REQUIRED}`;
const chatTypeRequired = `${REQUIRED}`;


export {
    rekamMedisUuidRequired,
    idRequired,
    alasanRequired,
    sessionIdRequired,
    keyRequired,
    messageRequired,
    nameRequired,
    userUuidRequired,
    chatTypeRequired
};
